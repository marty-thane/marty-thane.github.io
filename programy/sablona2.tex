% Knizni sablona v TeXu, vhodna pro sbirky povidek apod. Velikost papiru A5.
% Kapitoly piste mimo a vkladejte pomoci \input{}. Pro kompilaci pouzijte
% `pdflatex`.

% trida dokumentu
\documentclass[11pt,a5paper]{book}

% format pro archivaci
\usepackage[a-1b]{pdfx}

% kodovani
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% jazyk
\usepackage[czech]{babel} 

% pismo
\usepackage{tgtermes}

% nastaveni okraju
\usepackage[
	% papersize={153mm,215mm},
	layout=a5paper,
	% layouthoffset=2.5mm,
	% layoutvoffset=2.5mm,
	bindingoffset=6mm,
	margin=15mm,
	includefoot,
	% showframe
	]{geometry}

% radkovani
\linespread{1.1}

% vertikalni zarovnani
\raggedbottom

% microtyping
\usepackage[babel]{microtype}

% formatovani nadpisu
\usepackage{titlesec}
\titleformat{\chapter}[hang]
	{\normalfont\Huge\bfseries}{}{0pt}{}
\titleformat{\section}
	{\normalfont\Large\bfseries\centering}{\Roman{section}.}{0pt}{}

% polozky obsahu bez cislovani, normalni font
\usepackage{tocloft}
\renewcommand{\cftchapfont}{\normalfont}
\renewcommand{\cftchappagefont}{\normalfont}
\makeatletter
	\renewcommand{\cftchappresnum}{\begin{lrbox}{\@tempboxa}}
	\renewcommand{\cftchapaftersnum}{\end{lrbox}}
\makeatother
\setlength{\cftchapnumwidth}{0pt}

% v obsahu pouze kapitoly
\setcounter{tocdepth}{0}

% prazdne strany bez cislovani
\usepackage{emptypage}

% cislovani stran respektuje sudost/lichost
\usepackage{fancyhdr}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancypagestyle{plain}{
    \fancyfoot[LE,RO]{\thepage}
}

\begin{document}
\pagestyle{empty}

% titulni strana
\begin{center}
	\vspace*{\fill}
	\Huge\bf
	Jan Novák

	Kniha
	% \vspace{0.5em}
	\vspace{\fill}
\end{center}
\clearpage

% copyright
\vspace*{\fill}
{ 
	\setlength\parindent{0pt}
	% 1. revidované vydání.

	© Jan novák \the\year
}
\clearpage

% venovani
\begin{flushright}
	\vspace*{\fill}
	\it
	Zde je místo

	pro hezké věnování.
	\par\vspace{\fill}
\end{flushright}
\cleardoublepage

% obsah
{
	\microtypesetup{protrusion=false}
	\tableofcontents
}
\thispagestyle{empty}

% samotny text
\pagestyle{plain}
% \input{obsah/povidka1.tex}

%* tady bude treba opravit cislovani, pokud podekovani na vic stran
\input{content/podekovani.tex}
\thispagestyle{empty}

\end{document}
